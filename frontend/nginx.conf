# nginx config for the built SPA. Serves static assets and falls back to
# index.html for client-side routes (React Router), so deep links like
# /assessment/<token> resolve correctly on refresh.
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Long-cache the hashed asset bundles; never cache index.html.
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    # SPA fallback: unknown paths render the app shell, not a 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
