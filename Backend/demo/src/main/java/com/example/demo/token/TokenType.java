package com.example.demo.token;

/**
 * The kinds of secure link a token can represent. Stored as its {@code name()}
 * in the tokens table.
 */
public enum TokenType {
    ASSESSMENT,
    INTERVIEW
}
